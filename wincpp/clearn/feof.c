#include <stdio.h>


int main()
{
	/*
  FILE * fp = fopen("test.txt", "r");
  char line[100];

  while( ! feof(fp) ) {
    fgets(line, sizeof(line), fp);
    fputs(line, stdout);
  }
  fclose(fp);
  return 0;
  */
}