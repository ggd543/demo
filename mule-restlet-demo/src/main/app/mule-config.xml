<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:spring="http://www.springframework.org/schema/beans"
      xmlns:http="http://www.mulesoft.org/schema/mule/http"
      xmlns:vm="http://www.mulesoft.org/schema/mule/vm"
      xmlns:restlet="http://www.mulesource.org/schema/mule/restlet"
      xsi:schemaLocation="
        http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/3.2/mule.xsd
        http://www.mulesoft.org/schema/mule/http http://www.mulesoft.org/schema/mule/http/3.2/mule-http.xsd
        http://www.mulesoft.org/schema/mule/vm http://www.mulesoft.org/schema/mule/vm/3.2/mule-vm.xsd
        http://www.mulesource.org/schema/mule/restlet http://www.mulesource.org/schema/mule/restlet/3.2/mule-restlet.xsd">

    <description>
        This configuration uses an HTTP endpoint to receive requests.
    </description>

    <!--<service name="customerService">-->
    <!--<inbound>-->
    <!--<inbound-endpoint address="http://localhost:63080"/>-->
    <!--</inbound>-->
    <!--<outbound>-->
    <!--<filtering-router>-->
    <!--<outbound-endpoint address="vm://orderQueue"/>-->
    <!--<restlet:uri-template-filter pattern="/orders/{orderId}"/>-->
    <!--</filtering-router>-->
    <!--<filtering-router>-->
    <!--<outbound-endpoint address="vm://userQueue"/>-->
    <!--<restlet:uri-template-filter pattern="/users"/>-->
    <!--</filtering-router>-->
    <!--</outbound>-->
    <!--</service>-->

   <flow name="fuxk">
		<inbound-endpoint address="http://localhost:9002">
        </inbound-endpoint>
		<restlet:component restlet-ref="helloWorld"/>
       
	</flow>
	<spring:bean id="helloWorld" class="com.ggd543.restlet.application.HelloWorldApplication"/>

</mule>
